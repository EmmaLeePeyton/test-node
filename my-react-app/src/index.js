import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  //react.strict = best version of react
  <React.StrictMode> 
    <p>Hi EmmaLee</p>
  </React.StrictMode>
);